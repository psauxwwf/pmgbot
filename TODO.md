- [x] добавь файл exclude.txt в котором будут указаны email которые не нужно импортировать на этапе импорта т.е. и через daemon и через import команды
      при этом файл должен читаться на каждом цикле для daemon режима
