---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

dotenv: ["build.env"]

vars:
  gobin: go
  ldflags: "-extldflags '-static' -w -s -buildid="
  gcflags: "all=-trimpath={{.PWD}} -dwarf=false -l"
  asmflags: "all=-trimpath={{.PWD}}"
  bin: "{{.PWD}}/bin"
  app: "pmgbot"
  app_bin: "{{.bin}}/{{.app}}"
  repo_name:
    sh: basename "{{.PWD}}"

env:
  COMPOSE_BAKE: true
  CGO_ENABLED: 0
  GOARCH: amd64
  GOOS: linux

tasks:
  setup:
    desc: set up the environment
    internal: true
    silent: true
    cmds:
      - mkdir -p "{{.bin}}"

  build:
    desc: common build
    deps:
      - setup
    cmds:
      - >
        {{.gobin}} build
        -tags netgo
        -ldflags="{{.ldflags}}"
        -trimpath
        -gcflags="{{.gcflags}}"
        -asmflags="{{.asmflags}}"
        -o "{{.app_bin}}"
        ./cmd/{{.app}}
      - strip "{{.app_bin}}"
      - objcopy --strip-unneeded "{{.app_bin}}"

  test:
    desc: run test
    cmds:
      - go fix ./...
      - go test ./...

  test:analyze:
    desc: run analyze command with spam.json
    silent: true
    preconditions:
      - test -f spam.json
    cmds:
      - go run ./cmd/pmgbot analyze --config pmgbot.yaml --json spam.json

  test:generate:
    desc: run generate command with spam.json
    silent: true
    preconditions:
      - test -f spam.json
    cmds:
      - go run ./cmd/pmgbot generate --config pmgbot.yaml --json spam.json
